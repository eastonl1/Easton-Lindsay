---
const { bookingUrl, email } = Astro.props;
---

<section class="py-16" id="contact">
	<div class="mx-auto max-w-6xl px-6">
		<div class="grid gap-10 lg:grid-cols-[1fr_1.1fr]">
			<div>
				<p class="text-sm font-semibold uppercase tracking-[0.3em] text-[color:var(--c-muted)]">Contact</p>
				<h2 class="mt-3 text-3xl font-semibold md:text-4xl">Let's make your admin lighter.</h2>
				<p class="mt-4 text-sm text-[color:var(--c-muted)]">
					If you are self-employed or run a small professional firm, this is built for you. Book a quick call or send details below.
				</p>
				<div class="mt-6 flex flex-col gap-3 text-sm text-[color:var(--c-muted)]">
					<a class="link-underline text-[color:var(--c-text)]" href={bookingUrl}>
						Book a quick call
					</a>
					<a class="link-underline text-[color:var(--c-text)]" href={`mailto:${email}`}>
						{email}
					</a>
				</div>
			</div>
			<div class="rounded-[24px] border border-[color:var(--c-border)] bg-[color:var(--c-surface)] p-6 shadow-[0_30px_70px_-60px_rgba(15,23,42,0.6)]">
				<form
					class="grid gap-4"
					name="contact"
					method="post"
					data-netlify="true"
					netlify-honeypot="bot-field"
				>
					<input type="hidden" name="form-name" value="contact" />
					<label class="hidden">
						Do not fill this out
						<input name="bot-field" />
					</label>
					<label class="grid gap-2 text-sm font-semibold text-[color:var(--c-text)]">
						Name
						<input class="rounded-full border border-[color:var(--c-border)] bg-white px-4 py-3 text-sm" type="text" name="name" required />
					</label>
					<label class="grid gap-2 text-sm font-semibold text-[color:var(--c-text)]">
						Email
						<input class="rounded-full border border-[color:var(--c-border)] bg-white px-4 py-3 text-sm" type="email" name="email" required />
					</label>
					<label class="grid gap-2 text-sm font-semibold text-[color:var(--c-text)]">
						Website (optional)
						<input class="rounded-full border border-[color:var(--c-border)] bg-white px-4 py-3 text-sm" type="url" name="website" />
					</label>
					<label class="grid gap-2 text-sm font-semibold text-[color:var(--c-text)]">
						What do you do?
						<input class="rounded-full border border-[color:var(--c-border)] bg-white px-4 py-3 text-sm" type="text" name="role" required />
					</label>
					<label class="grid gap-2 text-sm font-semibold text-[color:var(--c-text)]">
						What do you want to improve?
						<textarea class="min-h-[120px] rounded-[20px] border border-[color:var(--c-border)] bg-white px-4 py-3 text-sm" name="goals" required></textarea>
					</label>
					<button class="btn btn-primary w-full text-sm" type="submit">Send details</button>
				</form>
				<p class="mt-4 text-xs text-[color:var(--c-muted)]">
					Prefer email? Write to <a class="link-underline" href={`mailto:${email}`}>{email}</a>.
				</p>
			</div>
		</div>
	</div>
</section>
