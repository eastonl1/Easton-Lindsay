---
const { bookingUrl, email } = Astro.props;

const steps = [
	{
		title: "Free intro call or website audit",
		price: "Free",
		copy:
			"We start with a short call or a quick Loom audit to understand your business and whether a new site would actually help.",
		bullets: ["Fixed quote", "Fit to your needs"],
	},
	{
		title: "Custom professional website",
		price: "$1,200-$6,000 CAD",
		copy:
			"One-time build tailored to your services, content, and goals. You get a clear quote before any work begins.",
		bullets: [
			"5–7 page professional site tailored to your services",
			"Fast, mobile-friendly, and easy to use",
		],
	},
	{
		title: "Optional maintenance",
		price: "$75/month",
		copy:
			"Ongoing updates, small changes, and support so you don't have to think about your site again.",
		bullets: [
			"Hosting included",
			"Basic maintenance",
			"Basic traffic insights",
			"Small content changes",
			"Paid Monthly, Cancel anytime",
		],
	},
];
---

<section class="section-reveal py-16" id="pricing">
	<div class="mx-auto max-w-6xl px-6">
		<div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
			<div>
				<p class="text-sm font-semibold uppercase tracking-[0.3em] text-[color:var(--c-muted)]">How it works</p>
				<h2 class="mt-3 text-3xl font-semibold md:text-4xl">
					A simple, three-step path to launch.
				</h2>
			</div>
		</div>

		<div class="reveal-stagger mt-10 grid gap-6 md:grid-cols-3">
			{steps.map((step) => (
				<article class="rounded-[24px] border border-[color:var(--c-border)] bg-[color:var(--c-surface)] p-6 shadow-[0_30px_65px_-55px_rgba(15,23,42,0.55)] transition-colors duration-200 hover:border-[color:var(--c-accent)]">
					<h3 class="text-2xl font-semibold md:text-3xl">{step.title}</h3>
					<p class="mt-3 text-2xl font-semibold text-[color:var(--c-text)]">{step.price}</p>
					<p class="mt-3 text-sm text-[color:var(--c-muted)]">{step.copy}</p>
					{step.bullets && (
						<ul class="mt-4 space-y-2 text-sm text-[color:var(--c-text)]">
							{step.bullets.map((bullet) => (
								<li class="flex gap-2">
									<span class="mt-2 h-1.5 w-1.5 rounded-full bg-[color:var(--c-accent)]" aria-hidden="true"></span>
									<span>{bullet}</span>
								</li>
							))}
						</ul>
					)}
				</article>
			))}
		</div>

	</div>
</section>
